[package]
name = "splat-cli"
version = "0.50.0"
edition = "2024"
license = "MIT"

[[bin]]
name = "splat"
path = "src/main.rs"

[dependencies]
splat-core = { version = "0.50", path = "../splat-core" }

anyhow = "1"
clap = { version = "4.5", features = ["derive"] }
